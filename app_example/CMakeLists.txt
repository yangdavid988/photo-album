# Please refer to path/to/your/ameba_sdk/cmake/CMakeLists-template.cmake

# ===== Public: applies to ALL targets (including LVGL) =====
# Add project directory to global include path so config/*.h is findable
ameba_global_include(${CMAKE_CURRENT_SOURCE_DIR})
# Use project-specific lv_conf instead of SDK default
ameba_global_define(LV_CONF_PATH="config/lv_conf_project.h")

# ===== Screen selection =====
# T1720A only — configured in ../Kconfig / prj.conf (CONFIG_SCREEN_T1720A).
# The Kconfig pipeline generates platform_autoconf.h per core (KM4NS/KM4TZ)
# which provides the CONFIG_* defines to all compilation units.

# ===== Private: builds our target =====
set(private_sources)
ameba_list_append(private_sources
    # main/
    main/app_main.c
    # core/
    core/jpeg_decode.c
    # ui/
    ui/album_ui.c
    # storage/  (SD-card photo source over VFS + FatFS)
    storage/album_sd.c
    # hal/
    hal/backlight_ctrl.c
    hal/lcd/lcd_drv.c hal/lcd/lcdc_core.c hal/lcd/t1720a_cfg.c
    hal/touch/touch_gt911.c
    # assets/photos/  (JPEG data arrays, generated by tools/jpg2album.py)
    assets/photos/album_photos.c
)
ameba_add_internal_library(app_example
    p_SOURCES
        ${private_sources}
    p_INCLUDES
        ${c_CMPT_FWLIB_DIR}/jpeg_decoder/inc
)
